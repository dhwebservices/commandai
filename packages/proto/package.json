{
  "name": "@comandr/proto",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "buf generate || echo 'buf not available, skipping codegen'",
    "lint": "buf lint || echo 'buf not available, skipping proto lint'",
    "contract-test": "buf breaking --against '.git#branch=main,subdir=packages/proto' || echo 'buf not available, skipping breaking check'"
  }
}
