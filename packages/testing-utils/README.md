# @commandai/testing-utils

Fixtures, fake tenants (home + MSP-with-sub-tenants), mock capability
registry. Used by dev seed script and test suites so multi-tenancy is
exercised everywhere by default.

## Must never depend on
Any app or service package (schema is the one allowed exception).
