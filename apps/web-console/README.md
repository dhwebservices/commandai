# web-console

Multi-tenant web app (React/Vite). Phase 1: placeholder screen only,
confirms build pipeline + ui-kit wiring. No product UI yet.

## Must never depend on
Backend services directly — all data access via api-gateway.
