# Testing

Each capability has unit tests for risk_level/requires_confirmation and a
happy-path execute() call. `CapabilityRegistry` tests cover found/not-found
lookup. Coverage gate: 80% (trust-critical tier).
